type Query {
  participants: [Participant!]!
}

type Mutation {
  enrollParticipant: [EnrollParticipant!]!
}

type EnrollParticipant {
  name: String!
  trialId: String!
  height: String
  weight: String
  hasDiabetes: Boolean
  hadCovid: Boolean
}

type EnrollParticipantResponse {
  id: String!
  eligible: Boolean!
}

type GetParticipant {
  id: String!
  trialId: String
}

union Participant = GetParticipant | EnrollParticipant

type MedicalFile {
  id: String!
  participant: Participant!
  height: String
  weight: String
  participantId: String
  hasDiabetes: Boolean
  hadCovid: Boolean
}
